import React from 'react'

const MenuItem = (props)=>{
    const {menuItem} = props;
    console.log(menuItem);
    return(
        <div>
            {/* {menuItem.value} */}
        </div>
    )
}

export default MenuItem